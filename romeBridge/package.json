{
  "name": "@rome/bridgesdk",
  "version": "0.0.1",
  "description": "widget-parent & widget-widget communication inside Rome Terminal",
  "main": "./dist/index.js",
  "type": "module",
  "files": [
    "dist/"
  ],
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -Rf ./dist && rm -Rf *.tgz",
    "prebuild": "yarn run clean",
    "build:tsup": "tsup",
    "build": "yarn build:tsup",
    "publish": "yarn build && npm publish --access=public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tsup": "^5.11.13",
    "typescript": "^4.6.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
